import React from "react";

const ServiceRequest = ({ match, results }) => {
  console.log(match.params.id);
  //   const Services = results.filter((result) => result._id !== match.params.id);

  return (
    <React.Fragment>
      <h1>hi</h1>
      <h1>hi</h1>
      <h1>hi</h1>
      <h1>hi</h1>
    </React.Fragment>
  );
};

export default ServiceRequest;
